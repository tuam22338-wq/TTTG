import React from 'react';

// This component is no longer used directly in CharacterPanel. 
// Its functionality has been merged into the new CharacterSheet design.
// It is kept temporarily to avoid breaking potential imports, but can be removed later.

const CoreStatsPanel: React.FC = () => {
    return null;
};

export default CoreStatsPanel;